spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/kurbrand?allowMultiQueries=true
    username: root
    password: 1234
  jpa:
    database: mysql
    # InnoDB설정
    database-platform: org.hibernate.dialect.MySQL8Dialect
    generate-ddl: false
    hibernate:
      ddl-auto: validate
    show-sql: true
    open-in-view: false
